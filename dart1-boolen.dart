import 'dart:math';

void main() {
  // You can use the num keyword to define digits that are either integers or

  num c = 7;
  num d = 5.8;
  var zambia = c + d;

  bool isBrowser = true;
  var isInvisble = false;

  print(zambia); // 12.8
  print(isBrowser || isInvisble); //true
  print(isBrowser && isInvisble); // false
}
